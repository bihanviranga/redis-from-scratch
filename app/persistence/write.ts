import { EMPTY_RDB_FILE } from "../consts/rdb";

/**
 * Returns the whole database in RDB format.
 * TODO: actually implement this. Currently it is returning an empty RDB file
 * that is hard-coded.
 */
export function getDatabaseAsRDB() {
  return EMPTY_RDB_FILE;
}
